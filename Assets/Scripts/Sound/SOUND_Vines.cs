using UnityEngine;
using System.Collections;

public class SOUND_Vines : MonoBehaviour {

	// Use this for initialization
	void Start () {
//		SoundManager.Instance.PlayEvent("Liana_Active", gameObject);
	}

	// Update is called once per frame
	void Update () {

	}
}
